[package]
name = "font_kit_wrapper"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"] # Creates dynamic lib
#crate-type = ["staticlib"] # Creates static lib

[dependencies]
font-kit = "0.11.0"

[build-dependencies]
cbindgen = "0.24.3"
